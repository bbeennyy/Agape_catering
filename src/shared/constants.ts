export const CHARGE_UNITS = ["FLAT", "PER_PERSON", "PER_UNIT"] as const;
export const PRICE_UNITS = ["PER_PERSON", "FLAT", "NONE"] as const;
export const LINE_TYPES = [
  "PER_PERSON",
  "FLAT",
  "PERCENT_DISCOUNT",
  "FIXED_DISCOUNT",
  "TBD",
] as const;
