export const CHARGE_UNITS = ["FLAT", "PER_PERSON", "PER_UNIT", "PER_SERVER"] as const;
export const PRICE_UNITS = ["PER_PERSON", "FLAT", "PER_LAYER", "NONE"] as const;
export const LINE_TYPES = [
  "PER_PERSON",
  "FLAT",
  "PERCENT_DISCOUNT",
  "FIXED_DISCOUNT",
  "TBD",
] as const;
